__pycache__/
*.pyc

# Model weights and downloaded binaries -- large, re-downloadable, don't belong in git
models/
piper_models/
whisper_cpp_models/

# Experimental venv + extracted CUDA DLLs from the GPU-acceleration investigation
venv311/
extra_dlls/
cudnn9/
cudnn9_download/

# Local-only secrets / per-deployment config
.turnstone_token
.turnstone_console_base
*.token

# Per-machine user preference (VAD sensitivity etc.), not project config
voice_config.json

# Written only when launched with no console attached (e.g. via
# create_shortcut.ps1's Start Menu shortcut, which uses pythonw.exe) --
# see assistant_app.py's sys.stdout redirect at the top of the file
orbit.log
