__pycache__/
*.pyc

# Model weights and downloaded binaries -- large, re-downloadable, don't belong in git
models/
piper_models/
whisper_cpp_models/

# Experimental venv + extracted CUDA DLLs from the GPU-acceleration investigation
venv311/
extra_dlls/
cudnn9/
cudnn9_download/

# Local-only secrets / per-deployment config
.turnstone_token
.turnstone_console_base
*.token

# Per-machine user preference (VAD sensitivity etc.), not project config
voice_config.json
